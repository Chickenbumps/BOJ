#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;


int main(){
  while(1){
    int k;
    cin >> k;
    if(k == 0) break;
    vector<int> v(k);
    fill(v.begin(),v.end(),0);
    for (int i = 0; i < k; i++) {
      int temp;
      cin >> temp;
      v.push_back(temp);
    }
    do{
     1111110
     11111
    }while(next_permutation(v.begin(),v.end())

  }


}
