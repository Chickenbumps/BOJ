{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "bulid",
            "type": "shell",
            "command": "g++ -g -std=c++14 main.cpp -o main",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}